[project]
name = "hash-miner"
version = "0.1.0"
description = "Native CPU miner for HASH token"
requires-python = ">=3.11"
dependencies = [
  "web3>=7.0.0",
  "pycryptodome>=3.20.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
